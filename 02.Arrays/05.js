function solve(arr){
    // let result = [];

    // for (let i = 0; i < arr.length; i++) {
    //     let el = arr[i];

    //     if(el >= result[result.length-1] || result.length === 0){
    //         result.push(el);
    //     }
    // }

    // return result;
    
    let result = arr.reduce(function(result, ){}, [arr[0]])
}